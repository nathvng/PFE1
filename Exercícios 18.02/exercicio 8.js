//8

//var:Escopo de função: O var possui escopo de função, se for declarado dentro de uma função, ele só será acessível dentro dessa função.


//let:Escopo de bloco: O let possui escopo de bloco, ou seja, ele só estará disponível dentro do bloco onde foi declarado (por exemplo, dentro de um if, for, ou uma função).


//const:Escopo de bloco: Assim como let, o const também possui escopo de bloco. Não pode ser redeclarado: Assim como o let, o const não pode ser redeclarado no mesmo escopo.
